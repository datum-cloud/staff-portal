import { HeartPulse } from 'lucide-react';

export default function IncidentsPage() {
  return (
    <iframe
      src="https://incidents.localhost:30443"
      className="h-full w-full border-0"
      title="Incidents"
    />
  );
}
